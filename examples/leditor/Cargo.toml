[package]
name = "leditor"
version = "0.1.0"
edition = "2024"

[dependencies]
lgui = { path = "../..", default-features = false, features = ["renderer-skia-software", "widgets", "dialogs"] }
icondata = "0.7"
