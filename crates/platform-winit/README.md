# LGUI Winit Backend

`lgui-platform-winit` owns LGUI's portable desktop event loop, windows, input translation,
accessibility integration, frame scheduling, and Skia surface drivers.

Applications normally enable it through the `lgui` facade.
