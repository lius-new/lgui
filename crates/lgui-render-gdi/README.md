# lgui-render-gdi

`lgui-render-gdi` implements the LGUI scene renderer with Windows GDI. Enable `renderer-gdi` on
the `lgui` facade for application use. The optional `advanced-rendering` feature adds retained
layers, SVG, image, blur, and richer primitive support.
