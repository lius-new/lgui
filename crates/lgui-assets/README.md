# LGUI Assets

`lgui-assets` owns image loading and caching, render resources, custom paint providers, and SVG
icon registration for LGUI. It depends on `lgui-core` for scene and memory contracts.

Applications should normally enable the corresponding features on `lgui`.

