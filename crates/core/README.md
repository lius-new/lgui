# LGUI Core

`lgui-core` contains LGUI's application runtime, retained component tree, layout, input, scene,
window, resource, and service contracts. It is an implementation package for the `lgui` facade.

Applications should normally depend on [`lgui`](https://crates.io/crates/lgui) instead of using
this package directly.
