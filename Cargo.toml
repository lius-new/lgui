[package]
name = "lgui"
version = "0.1.0"
edition = "2021"
publish = false
readme = "README.md"

[features]
default = ["router", "store", "widgets"]
async = []
router = []
store = []
theme = []
tokio = ["async", "dep:tokio"]
widgets = ["theme"]

[dependencies]
tokio = { version = "1", optional = true, features = ["rt"] }
